---
- hosts: all
  roles:
    - install-utils
    - base-security # fail2ban, firewall
    # - syslog
    # - postfix
    - deploy-user
    # - disable root user completely
# now switch to deploy user. split playbook file based on server type?
# - nginx
# - checkout dot-files
# - build static sites
# - set up all dynamic apps:
#   - mailinator-clone
#   - git hooks for heroku-like auto apps
